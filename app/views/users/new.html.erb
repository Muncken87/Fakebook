<h1>Create User</h1>
<%= form_for @user, url: {action: "create"}, html: {class: "create_user"} do |f| %>

  <%= f.label :first_name %>
  <%= f.text_field :first_name %>

  <%= f.label :last_name %>
  <%= f.text_field :last_name %>

  <%= f.submit :create %>

<% end %>
